'use strict';

/**
 * interns-analytics service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::interns-analytics.interns-analytics');
